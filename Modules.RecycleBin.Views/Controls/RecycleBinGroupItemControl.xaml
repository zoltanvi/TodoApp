<UserControl
    x:Class="Modules.RecycleBin.Views.Controls.RecycleBinGroupItemControl"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:local="clr-namespace:Modules.RecycleBin.Views.Controls"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    d:DataContext="{d:DesignInstance local:RecycleBinGroupItemViewModel}"
    d:DesignHeight="450"
    d:DesignWidth="800"
    mc:Ignorable="d">
    <Grid>
        <Border
            Margin="8 1 0 0"
            BorderBrush="{DynamicResource SecondaryContainer}"
            BorderThickness="1 0 0 0"
            CornerRadius="0">

            <Grid>
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto" />
                    <RowDefinition Height="Auto" />
                    <RowDefinition Height="Auto" />
                    <RowDefinition Height="*" />
                </Grid.RowDefinitions>

                <!--  CATEGORY GROUP TITLE  -->
                <Grid Grid.Row="0">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*" />
                        <ColumnDefinition Width="Auto" />
                        <ColumnDefinition Width="Auto" />
                    </Grid.ColumnDefinitions>

                    <!--  CATEGORY NAME  -->
                    <Border
                        Grid.Column="0"
                        Background="{DynamicResource Surface3}"
                        CornerRadius="0"
                        
                        />

                    <Button
                        Grid.Column="0"
                        Padding="6 0"
                        Command="{Binding ToggleGroupIsOpen}"
                        Content="{Binding CategoryName}"
                        Style="{StaticResource GroupItemTitleButtonStyle}"
                        ToolTip="{Binding CategoryName}" />

                    <Border Background="Transparent"
                            Grid.Column="1"
                            Width="{Binding UIScaler.FontSize.Large, Source={StaticResource UIScaler}}"
                            Margin="2 0" />

                    <!--  RESTORE CATEGORY  -->
                    <Button
                        Grid.Column="2"
                        Margin="2 0"
                        Width="{Binding UIScaler.FontSize.Large, Source={StaticResource UIScaler}}"
                        Height="{Binding UIScaler.FontSize.Large, Source={StaticResource UIScaler}}"
                        FontSize="{Binding UIScaler.FontSize.Small, Source={StaticResource UIScaler}}"
                        VerticalAlignment="Center"
                        Command="{Binding RestoreFullCategoryCommand}"
                        Content="{StaticResource IconRestore}"
                        Style="{StaticResource ButtonQuickActionStyle}"
                        ToolTip="Restore all tasks in category" />
                </Grid>

                <Border
                    Grid.Row="1"
                    Height="1"
                    Margin="0 1"
                    Visibility="{Binding IsOpen, Converter={StaticResource BoolToVisibilityConverter}}"
                    />

                <!--  CHILD CATEGORY GROUPS (recursive)  -->
                <ItemsControl
                    Grid.Row="2"
                    ItemsSource="{Binding Children}"
                    Visibility="{Binding IsOpen, Converter={StaticResource BoolToVisibilityConverter}}">
                    <ItemsControl.ItemTemplate>
                        <DataTemplate DataType="{x:Type local:RecycleBinGroupItemViewModel}">
                            <local:RecycleBinGroupItemControl />
                        </DataTemplate>
                    </ItemsControl.ItemTemplate>
                </ItemsControl>

                <!--  TASK ITEMS  -->
                <ListView
                    x:Name="GroupItemListView"
                    Grid.Row="3"
                    HorizontalContentAlignment="Stretch"
                    Background="Transparent"
                    BorderThickness="0"
                    ItemsSource="{Binding Items}"
                    ScrollViewer.CanContentScroll="False"
                    ScrollViewer.HorizontalScrollBarVisibility="Disabled"
                    ScrollViewer.VerticalScrollBarVisibility="Disabled"
                    SelectionMode="Single"
                    Template="{StaticResource NonScrollableListBoxControlTemplate}"
                    VirtualizingPanel.IsContainerVirtualizable="True"
                    VirtualizingPanel.IsVirtualizing="True"
                    VirtualizingPanel.ScrollUnit="Pixel"
                    VirtualizingPanel.VirtualizationMode="Recycling"
                    Visibility="{Binding IsOpen, Converter={StaticResource BoolToVisibilityConverter}}">

                    <!--  Enabling virtualization is really important  -->
                    <!--  https://stackoverflow.com/questions/3724593/why-setting-scrollviewer-cancontentscroll-to-false-disable-virtualization  -->
                    <ListView.ItemsPanel>
                        <ItemsPanelTemplate>
                            <VirtualizingStackPanel />
                        </ItemsPanelTemplate>
                    </ListView.ItemsPanel>

                    <ListView.ItemContainerStyle>
                        <Style TargetType="ListViewItem">
                            <Setter Property="Template">
                                <Setter.Value>
                                    <ControlTemplate TargetType="ListViewItem">
                                        <ContentPresenter />
                                    </ControlTemplate>
                                </Setter.Value>
                            </Setter>
                            <Setter Property="Foreground" Value="{DynamicResource OnBackground}" />
                            <Setter Property="FocusVisualStyle" Value="{x:Null}" />
                        </Style>
                    </ListView.ItemContainerStyle>

                    <ListView.ItemTemplate>
                        <DataTemplate>
                            <local:RecycleBinTaskItemControl />
                        </DataTemplate>
                    </ListView.ItemTemplate>
                </ListView>

            </Grid>



        </Border>

    </Grid>
</UserControl>
